a <- 25
b <- 4

x <- 10
cat("Natural log of", x, "is:", log(x), "\n") 
cat("Log base 10 of", x, "is:", log10(x), "\n") 
cat("Exponential of", x, "is:", exp(x), "\n") 

theta <- pi / 4 
cat("Sin of", theta, "radians is:", sin(theta), "\n") 
cat("Cos of", theta, "radians is:", cos(theta), "\n") 
cat("Tan of", theta, "radians is:", tan(theta), "\n") 

num <- 3.14159
cat("Ceiling of", num, "is:", ceiling(num), "\n") 
cat("Floor of", num, "is:", floor(num), "\n") 
cat("Rounded value of", num, "is:", round(num, 2), "\n") 

y <- -16
cat("Square root of", a, "is:", sqrt(a), "\n") 
cat("Absolute value of", y, "is:", abs(y), "\n") 

n <- 5
cat("Factorial of", n, "is:", factorial(n), "\n") 
cat("Combination of 5 choose 2 is:", choose(5, 2), "\n") 
